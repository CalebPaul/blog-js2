<div class="well">
  <div class="row">
    <div class="col-md-8">
      {{post.imageFeature}}
    </div>
    <div class="col-md-4">
      <h2>{{post.title}}</h2>
      <h4>{{post.author}}<small>{{post.timestamp}}</small></h4>
      {{post.teaser}}
    </div>
  </div>
</div>
